# PyInstaller
build/
dist/
dist-installer/
dist_data/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
env/
.env

# Secrets — generated at build time, never commit
writ_key.py
WRIT_DB_KEY
build_encrypted_db.py
build_exe.sh

# Fetched at setup time, too big for git
models/

# Data (raw corpus is local-only; curated notes + golden set ARE committed)
*.db
*.sqlite
*.jsonl
*.parquet
*.csv
!data_curated/*.jsonl
!eval/golden.jsonl

# Eval outputs (regenerated by running the harness)
eval/results_*.json

# OS generated
.DS_Store
Thumbs.db

# Local PoC eval harness (prompts, runs, reports stay on this machine)
eval/
